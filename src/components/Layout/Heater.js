import { Fragment } from 'react';

// import classes from './Header.module.css';

const Header = (props) => {
  return (
    <Fragment>
      <header>
        <h1>Happy Foods </h1>
      </header>
      <div></div>
    </Fragment>
  );
};

export default Header;